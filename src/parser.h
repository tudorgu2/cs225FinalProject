#include <ifstream>
#include <vector> 
#include <unordered_map> 

using namespace std;
using namespace cs225;


class parser{
    public:
        Parse();
        Parse(const string& routes, const string& airports);
        

}
